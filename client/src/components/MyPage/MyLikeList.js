import React, {Component} from 'react';
import './MyLikeList.css';

const MyLikeList = ({form, children}) => {
    return (
        <section className = "like-list-wrapper">
            LIKED
        </section>
    );
};

export default MyLikeList;
